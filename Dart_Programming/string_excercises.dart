void main() {
  //Given the string below, replace all
  // 'x's with a blank

  var errorMessage = "Uxh oxh, thexre's a bunch of txypos";
  var correctMessage = errorMessage.replaceAll('x', '');
  print(correctMessage);
}
